export interface employeeType {
  id: string;
  name: string;
  department: string;
  salary: number;
}

export interface authType {
  email: string;
  username: string;
  password: number;
}

export interface AddEmployee {
  name: string;
  department: string;
  salary: number;
}